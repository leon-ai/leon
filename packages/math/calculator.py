
from bridges.python import utils

def run(string, entities):
  
  #equation = entities['sourceText']

  #results = entities[0]['entity']

  #results = eval(equation)

  return utils.output('end', 'results', 'results')