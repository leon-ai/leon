import { type IconButtonProps } from '@leon-ai/aurora'

import { Widget } from '../widget'

export class IconButton extends Widget<IconButtonProps> {
  constructor(props: IconButtonProps) {
    super(props)
  }
}
