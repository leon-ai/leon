import { type ProgressProps } from '@leon-ai/aurora'

import { Widget } from '../widget'

export class Progress extends Widget<ProgressProps> {
  constructor(props: ProgressProps) {
    super(props)
  }
}
