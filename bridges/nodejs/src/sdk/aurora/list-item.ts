import { type ListItemProps } from '@leon-ai/aurora'

import { Widget } from '../widget'

export class ListItem extends Widget<ListItemProps> {
  constructor(props: ListItemProps) {
    super(props)
  }
}
