import { type TabGroupProps } from '@leon-ai/aurora'

import { Widget } from '../widget'

export class TabGroup extends Widget<TabGroupProps> {
  constructor(props: TabGroupProps) {
    super(props)
  }
}
