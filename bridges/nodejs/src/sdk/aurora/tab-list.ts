import { type TabListProps } from '@leon-ai/aurora'

import { Widget } from '../widget'

export class TabList extends Widget<TabListProps> {
  constructor(props: TabListProps) {
    super(props)
  }
}
